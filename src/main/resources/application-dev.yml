
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/blog?serverTimezone=CTT&useUnicode=true&characterEncoding=utf-8
    username: root
    password: 123456

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    #防止延迟加载
    open-in-view: true
logging:
  level:
    root: info
    com.hanj: debug
  file: log/blog-dev.log
